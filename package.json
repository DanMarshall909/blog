{
	"scripts": {
		"sync-article-assets": "node scripts/sync-article-assets.mjs",
		"prebuild": "npm run sync-article-assets",
		"dev": "astro dev --port 3000",
		"build": "astro build && npm run minify-built-css",
		"postbuild": "node scripts/generate-pdfs.mjs",
		"minify-built-css": "node scripts/minify-built-css.mjs",
		"pdfs": "node scripts/generate-pdfs.mjs",
		"preview": "astro preview",
		"test": "playwright test"
	},
	"dependencies": {
		"@astrojs/rss": "^4.0.19",
		"astro": "^7.1.3"
	},
	"private": false,
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"esbuild": "0.28.1"
	},
	"overrides": {
		"esbuild": "0.28.1"
	}
}
